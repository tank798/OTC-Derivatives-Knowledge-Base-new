# 手动问答核验

`queries.jsonl` 只保留三道真实用户问题，不包含预期结论、必须命中的法规、固定 Chunk ID 或正则判定条件。

核验方式：

1. 启动本地 API 和前端。
2. 在聊天页面输入一道问题。
3. 检查 Agent 的问题改写，确认或直接纠正。
4. 阅读最终回答、法规原文和官网链接。
5. 必要时查看 `data/index/eval/logs/` 中的运行日志，确认检索轮次、保留证据、引用校验和模型调用耗时。

三道问题不拥有专项代码或专项 Prompt，与其他真实问题使用同一套对话式 Agent 逻辑。

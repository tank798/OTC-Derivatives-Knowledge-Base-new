# 端到端问答评测集

这里的三个问题走与正式产品完全相同的链路：问题基础分析、DeepSeek 检索规划、混合检索工具调用、证据充分性判断、必要时补检、DeepSeek 回答、程序校验和独立 DeepSeek 审查。

`queries.jsonl` 不预置 Chunk ID，也不指定哪个 Chunk 必须被引用。模型从本次实际检索上下文中自行选择证据，评测器检查最终判断、适用范围、必要法规、关键限定、逐字原文、官网链接、程序校验、审查结论和循环次数。

配置 `LLM_API_KEY`、`LLM_MODEL=deepseek-v4-pro`、`LLM_FAST_MODEL=deepseek-v4-flash` 后运行：

```bash
pnpm eval:qa
```

本地运行会生成（不提交 Git）：

- `results.json`：保存检索计划、实际模型上下文、证据判断、最终回答、模型选择的引用、程序校验、独立审查和受限状态轨迹。
- `results.md`：按“先直接回答、后法规依据及官网链接”的格式生成简表。

这两个文件只用于本地调试并被 Git 忽略。普通前端不会展示 Chunk ID、检索排名、内部轨迹或审查过程。

## 当前真实复跑结果

2026年7月13日使用 `deepseek-v4-flash` 规划、`deepseek-v4-pro` 完成证据判断、回答和审查，并通过正式 `ComplianceService` 在1,228个 Chunk 上完成评测：

| 问题 | 直接回答 | 检索轮次 | 修订次数 | 审查次数 | 模型调用 | 结果 |
|---|---|---:|---:|---:|---:|---|
| 上市公司挂钩自身股票 | 不能确认 | 2 | 0 | 1 | 5 | PASS |
| 券商收益凭证做雪球 | 不能确认 | 2 | 0 | 1 | 5 | PASS |
| 私募产品投资雪球比例 | 是 | 1 | 0 | 1 | 4 | PASS |

三题合计14次模型调用，程序化真实性校验和独立模型审查均通过。生产代码没有按题目名称或固定 Chunk 设置专项分支；三题使用与正式问答相同的通用链路，最终上下文最多包含10个 Chunk。

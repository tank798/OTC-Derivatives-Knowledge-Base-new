# 迁移与清理说明

## 已迁移

- 176 份当前法规原件；
- 已复核的结构化切分程序；
- 1,828 条最终 Chunk；
- 文档元数据、BM25、向量矩阵和重复审计；
- NestJS API、Next.js Web UI 和共享类型；
- 混合检索、证据回答、引用校验和测试。

## 未迁移

- 早期网页爬虫和各站点专用抓取脚本；
- 历史 HTML/PDF/TXT 抓取缓存；
- 旧 `data/processed/clauses.jsonl` 与旧证据账本；
- Wiki 生成脚本和旧 Wiki；
- Claude/DeepSeek 复核过程目录与临时输出；
- 日志、缓存、构建产物、虚拟环境、IDE 文件；
- 与新主流程无引用关系的调试脚本。

没有无法判断用途而必须迁移的旧代码，因此新项目不设置 `legacy/`。旧项目仍保留在原位置，可用于历史追溯。
